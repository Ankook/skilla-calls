import React from 'react';
import styles from "./Header.module.scss";


export interface ISidebarProps {
};

const Header: React.FC<ISidebarProps> = ({  }) => {
  return (
    <div className={styles.header}>
      
    </div>
      )
}
export { Header }